### doodle -- you can close your git bash window with nohup


> example

```
nohup doodle --style style-space-illustrated.png --output content-thomas-tank-engine.png --device=cpu --iterations=40 &
```

Prefix command with `nohup` and end with `&`


